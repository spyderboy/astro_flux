["import 'package:flutter_riverpod/flutter_riverpod.dart';", "import 'package:test/test.dart';", "import 'package:astro_flux/components/combat_attack_handler.dart';", "import 'package:astro_flux/models/game_state_provider.dart';", 'void main() {', "  test('mote-star hit within radius correctly detected', () {", '    final ref = ProviderContainer();', '    final gameStateProvider = StateNotifierProvider<GameStateNotifier, GameState>((ref) => GameStateNotifier());', '    ref.read(gameStateProvider.notifier).addStar(id: 1, tier: 1);', '    ref.read(gameStateProvider.notifier).addVector(id: 2);', '    final handler = CombatAttackHandler(ref);', '    handler.ref.watch(gameStateProvider).vectors.first.position = vm.Vector2(0, 0);', '    handler.ref.watch(gameStateProvider).stars.first.position = vm.Vector2(15, 0);', '    final event = CombatAttackEvent(sourceVectorId: 2, targetStarId: 1);', '    expect(handler.handleEvent(event), true);', '  });']