name: astro_flux
description: A new Flutter project.

publish_to: none

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flame: ">=1.0.0 <2.0.0"
  flame_audio: ">=2.0.0 <3.0.0"
  flutter_riverpod: "^3.3.1"
  vector_math: ">=2.1.1 <3.0.0"
  connectivity_plus: ">=5.0.0 <7.0.0"
  firebase_core: "^3.0.0"
  cloud_firestore: "^5.0.0"
  cloud_functions: "^5.6.2"

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ">=2.0.0 <7.0.0"

flutter:
  uses-material-design: true
  assets:
    - assets/audio/
