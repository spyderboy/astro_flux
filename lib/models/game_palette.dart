// ignore_for_file: depend_on_referenced_packages
import 'dart:ui';

/// A collection of high-saturation neon colors used throughout the game.
/// All colors are designed to be vibrant against a dark background.
class GamePalette {
  /// Cyan neon
  static const Color cyan = Color(0xFF00FFFF);

  /// Magenta neon
  static const Color magenta = Color(0xFFFF00FF);

  /// Lime neon
  static const Color lime = Color(0xFF00FF00);

  /// Yellow neon
  static const Color yellow = Color(0xFFFFFF00);

  /// Orange neon
  static const Color orange = Color(0xFFFFA500);

  /// Pink neon
  static const Color pink = Color(0xFFFF69B4);

  /// Blue neon
  static const Color blue = Color(0xFF0000FF);

  /// Electric Purple neon
  static const Color purple = Color(0xFFBF00FF);

  /// A default vibrant color for UI elements or particles.
  static const Color primary = cyan;

  /// A secondary vibrant color for secondary effects.
  static const Color secondary = magenta;
}